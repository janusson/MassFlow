"""Benchmark and experiment scripts for MassFlow.

Scripts in this directory are not part of the installed package; they are
runnable directly (``python scripts/<name>.py``) and importable as
``scripts.<name>`` from tests.
"""
