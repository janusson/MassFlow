# Conversion API

This module provides programmatic access to the vendor file conversion helpers.
It wraps ProteoWizard's `msconvert` to seamlessly translate proprietary `.raw`
and `.d` formats into open `.mzML` standards.

::: MassFlow.convert
