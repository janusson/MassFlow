# Configuration API

MassFlow relies heavily on Pydantic models to validate the `massflow_config.yaml`
file. This module defines the strict schemas for data inputs, processing
thresholds, and ML similarity parameters.

::: MassFlow.config
