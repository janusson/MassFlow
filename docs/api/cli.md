# Command Line Interface (CLI)

The `MassFlow.cli` module uses the `click` framework to provide the user-facing
terminal commands (`annotate`, `init`, `convert`, `db`). It handles argument
parsing and delegates execution to the core workflow.

::: MassFlow.cli
