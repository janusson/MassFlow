# Input / Output API

The `MassFlow.io` module manages the file-system boundary. It handles loading
spectra from mzML, MGF, MSP, and SQLite, and orchestrates the export of your
final annotation results to CSV, JSON, Excel, and Parquet formats.

::: MassFlow.io
