# Similarity Engines

The `MassFlow.similarity` module encapsulates the logic for comparing mass spectra.
It supports standard algorithms (Cosine) and Machine Learning models (Spec2Vec,
MS2DeepScore), as well as advanced routing via the CascadeEngine.

::: MassFlow.similarity
