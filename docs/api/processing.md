# Processing Pipeline

The `MassFlow.processing` module acts as a wrapper around the `matchms` library,
applying a strict sequence of metadata harmonization and peak-filtering rules
to incoming spectra.

::: MassFlow.processing
