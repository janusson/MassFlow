# Workflow Orchestrator

The `MassFlow.workflow` module coordinates the end-to-end execution path.
It dispatches similarity searches across parallel worker processes, manages
False Discovery Rate (FDR) calculations, and bridges inputs to outputs.

::: MassFlow.workflow
